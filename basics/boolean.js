// === equality
// !== not euaals
// < less than
// > greater than
// <= less than equal to
// >= greater than equal to

let temp = 132;

if (temp <= 32) {
  console.log("It is freezing outside");
}

if (temp >= 110) {
  console.log("It is way too hot outside");
}

// Discount challenge.
let age = 5;

if (age <= 7) {
  console.log("You are eligible for children discount.");
}

if (age >= 65) {
  console.log("You are eligible for senior citizen discount.");
}
