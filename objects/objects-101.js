let myBook = {
  title: "1984",
  author: "George Orwell",
  pageCount: 300,
};

console.log(
  `${myBook.title} is written by ${myBook.author} and is ${myBook.pageCount} pages long.`,
);

// Me challenge.

let me = {
  name: "Ashutosh",
  age: 26,
  location: "Mumbai",
};

console.log(`${me.name} is ${me.age} years old and lives in ${me.location}.`);
me.age += 1;
console.log(`${me.name} is ${me.age} years old and lives in ${me.location}.`);
